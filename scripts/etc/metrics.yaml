module:
    type: static
    metrics:
        request:
            desc: module request counter
        response:
            desc: module response counter

# every transcation have its own metrics
transcation:
    type: dynamic
    metrics:
        request:
            desc: transcation request count
        response:
            desc: transcation response count
