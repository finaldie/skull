example:
    request:
        desc: this is an example, request counter
    response:
        desc: this is an example, response counter

test:
    connection:
        desc: how many connection has been accepted
    uptime:
        desc: uptime counter
